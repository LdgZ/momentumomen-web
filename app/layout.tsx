import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "momentumomen — Wedding Content Creator Profesional",
  description: "Layanan wedding content creator profesional untuk mengabadikan momen spesial pernikahan Anda. Foto & video berkualitas tinggi, editing cinematic, dan hasil yang memukau. Bondowoso, Jawa Timur.",
  keywords: ["wedding", "fotografer pernikahan", "videografer", "content creator", "dokumentasi wedding", "momentumomen", "Bondowoso"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="id" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
        suppressHydrationWarning
      >
        <Header />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
